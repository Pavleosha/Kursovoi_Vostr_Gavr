# Преобразованная таблица Бутчера
a = np.array([
    [0, 0, 0],  # a0
    [0, 0, 0],  # a1
    [0, 1 / 2, 0],  # a2
])
b = np.array([0, 0, 1])  #нижняя строка
c = np.array([0, 0, 1 / 2])  #левый столбец
